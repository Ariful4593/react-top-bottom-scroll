import { default as React } from 'react';
import { ScrollUpDownProps } from '../types';

export type { ScrollDirection, UseScrollUpDownOptions, ButtonPosition, ScrollMode, DualLayout, ProgressBarPosition, ScrollUpDownProps, } from '../types';
declare const ScrollUpDown: React.FC<ScrollUpDownProps>;
export default ScrollUpDown;
